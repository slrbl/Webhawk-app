<p style="color: green"><%= notice %></p>

<%= render @incident %>

<div>
<br>

<% if @incident.user_id==current_user.id%>

          <%= button_to "Work on this incident", edit_incident_path(@incident), :method => :get, :class => "ui mini orange button" %>

<% end %>

 <!-- <%= link_to "Edit this incident", edit_incident_path(@incident) %> |
  <%= link_to "Back to incidents", incidents_path %>

  <%= button_to "Destroy this incident", @incident, method: :delete %>-->
</div>
